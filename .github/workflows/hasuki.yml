name: hasuki

on: [push, pull_request]

env:
  SERVERLESS_ACCESS_KEY: ${{ secrets.SERVERLESS_ACCESS_KEY }}

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Pages
        uses: actions/configure-pages@v1

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - uses: pnpm/action-setup@v2.2.2
        name: Install pnpm
        id: pnpm-install
        with:
          version: 7
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        run: |
          echo "::set-output name=pnpm_cache_dir::$(pnpm store path)"

      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ steps.pnpm-cache.outputs.pnpm_cache_dir }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install

      - run: cd ./packages/backend && pnpm build
      - run: cd ./packages/backend && pnpm test
      - run: cd ./packages/backend && pnpm package

      - run: cd ./packages/frontend && pnpm build

      - uses: actions/upload-artifact@v3
        with:
          name: frontend-artifact
          path: ./packages/frontend/dist

      - uses: actions/upload-pages-artifact@v1
        with:
          path: ./packages/frontend/dist

      # - uses: actions/upload-artifact@v3
      #   with:
      #     name: backend-artifact
      #     path: ./packages/backend/.serverless

  deploy_frontend:
    if: ${{ github.event_name == 'push' && (github.ref_name == 'main') }}

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1

  # deploy_backend:
  #   if: ${{ github.event_name == 'push' && (github.ref_name == 'main') }}

  #   needs: build
  #   runs-on: ubuntu-latest

  #   defaults:
  #     run:
  #       working-directory: ./packages/backend

  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Install Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: 16

  #     - uses: actions/download-artifact@v3
  #       with:
  #         name: backend-artifact
  #         path: ./packages/backend/.serverless

  #     - name: serverless deploy
  #       uses: serverless/github-action@v3.1
  #       with:
  #         args: -c "serverless plugin install --name serverless-scriptable-plugin && cd ./packages/backend && serverless deploy"
  #         entrypoint: /bin/sh
